#include "main.h"
/**
 * *_strstr - find first occurrence of substring in a string.
 * @haystack : string.
 * @needle : substring.
 * Return: pointer to first occurence.
 */
char *_strstr(char *haystack __attribute__((unused)),
		char *needle __attribute__((unused)))
{
	return (0);
}
